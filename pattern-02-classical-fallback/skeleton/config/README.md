# Configuration Skeleton

Configuration determines which optimization engines participate in execution.

Business logic never changes when introducing new technologies.

Only configuration changes.

Typical production systems externalize these values using environment variables, YAML files, or centralized configuration services.